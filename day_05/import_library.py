# instead of defining our own function why not we use the the function already defined by other experienced programmers
# these are called libraries
 
#to find value of pi
import math
print('value of pi is :' , math.pi)

#to find mean 
import statistics
data = [1, 2, 3, 4, 5]
print(statistics.mean(data))

#imp libraries numpy, pandas and soo many